[package]
name = "rocket"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "rocket"
doc = false

[dependencies]
tidos = { path = "../../../tidos", features = ["rocket"] }
common = { path = "../common" }
rocket = "0.5.1"