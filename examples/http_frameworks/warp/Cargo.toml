[package]
name = "warp"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "warp"
doc = false

[dependencies]
tidos = { path = "../../../tidos", features = ["warp"] }
warp = "0.4.2"
tokio = { version = "1", features = ["full"] }
