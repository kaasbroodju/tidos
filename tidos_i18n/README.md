# tidos_i18n

> **Internal crate** — this is a workspace dependency of [tidos](https://crates.io/crates/tidos).

`tidos_i18n` provides the `i18n!` procedural macro used for internationalization in the Tidos SSR framework. It is not intended to be used directly; enable the `i18n` feature flag on the `tidos` crate instead.

## Documentation

For the full documentation and usage examples, see the **[tidos crate on docs.rs](https://docs.rs/tidos)**.
